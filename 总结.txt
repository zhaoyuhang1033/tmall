本次模拟天猫电商网站主要运用以下技术:
HTML  CSS  JS JQuery 还有一个懒加载插件(代码仍然写的和流水账一样)
重点在于收集数据(手动收集)数组中存放对象
因为许多的商品区块在HTML结构和css 大致相同 , 仅数据不同 
将相同的部分抽象成一个HTML模板(运用到了ES6的模板字符串 很方便)
循环遍历数据(forEach) , 为父容器追加子元素(数据为主)  
在一个图片如此之多 , 寸土寸金的页面上 , 懒加载是非常有必要的(lazylode.js)
